# 핸드오프 — 2026-06-12 → 다음 세션

> ⚠️ **너는 새 Claude Code 세션(아키텍트)이다. 이 문서가 현재 상태 + 다음 작업이다.**
> 즉시: `pwd`(= `C:\dev\recipesss`, OneDrive면 멈춤) → `git pull` →
> `CLAUDE.md`/`AGENTS.md` → `SPEC.md`(§5, §13 결정로그 **DL-040까지**) → 이 문서 §3.

---

## 0. 역할 (그대로)
| | 담당 |
|---|---|
| **Claude (너)** | 아키텍트 — SPEC 관리, 리뷰, 위험분석. **작은 수정·버그픽스·UI 다듬기는 직접** 구현(이번 세션 내내 그렇게 함). |
| **호두(jieun@fantapet.com)** | 방향 결정, 배포(`npm run deploy:hosting`), 시각 검증. 코딩 직접은 안 함(바이브코딩). |

사이클: 직접 구현 → 게이트(`typecheck && lint && test && build`) → mojibake grep → 커밋 → push(호두 승인 시). **세션 시작 시 `git pull` 먼저**(호두/타 세션이 앞설 수 있음).

## 1. 현재 상태 (origin/main = `a0b21a3`, 동기화됨)

**단계 0~4 전부 완료 + 추가 기능 다수.** 핵심 흐름 실사용 검증됨:
**원료(USDA/수동) → 레시피 편집(구성·헤더·카테고리) → 영양 매트릭스/확정값 → 등록(생산앱 푸시) → 발주 → PDF 출력/저장.**

- **테스트 173개 통과**, typecheck/lint/build green. 배포: `recipesss-app.web.app`(호두가 `npm run deploy:hosting`).
- 스택: React18 + TS(strict, noUncheckedIndexedAccess, verbatimModuleSyntax) + Vite + Firebase(fant-e5ae5). 순수로직 `src/features/*` + Vitest, 화면 `src/pages/*`.

**이번 세션에 한 것(요약):**
- 병합보강(DL-034)·프리셋 재설계(DL-035)·USDA 가져오기+페이지네이션·등록(DL-037)·1-D 매트릭스/확정값(DL-027~029)·구성편집·헤더편집·신규레시피·카테고리(생식/동결건조/동결텐더)·**단계4 PDF 출력1·2(DL-038)**·**발주 저장/재출력(DL-039)**·매트릭스/목록/확정값 컴팩트화·프리셋 투입량 백필·대시보드 제거·**규칙 정본 정책 정정(DL-040)**.

## 2. 🔴 블로커 — 호두 액션 1건 (코드 아님)

**발주 저장(`recipesssOrders`)이 권한오류로 안 됨.** 컬렉션 규칙이 라이브에 없어서. 새 정책(DL-040)상 recipesss에서 배포 ❌ → **재고관리(fantapet-inventory) 정본에 아래 블록 추가 후 재고관리에서 1회 배포**:
```
match /recipesssOrders/{uid}/items/{orderId} {
  allow read, write: if signedIn() && request.auth.uid == uid;
}
```
→ 배포되면 발주 저장/조회/PDF 드롭다운 즉시 작동. (그 전까지 PrintPage에 빨간 에러 안내 뜸 — 정상.)

## 3. 다음 작업 후보 (호두가 고르게)

핵심은 다 끝나서 남은 건 마무리·polish:
- **단계 5 — 단가 관리(`/prices`)**: placeholder. 단, **생산앱과 인터페이스 협의 필요(DL-024)** — 협의 전엔 못 함. 호두에게 협의 여부 확인.
- **드래그 정렬(DL-022)**: 구성 원료·레시피 목록이 지금 ↑↓ 버튼. `@dnd-kit`(설치됨)으로 드래그.
- **백업·복원(`/settings`)**: 이미 SettingsPage 있음 — 마이그레이션/스냅샷 점검·다듬기.
- **PDF/발주 추가 다듬기**: 호두가 실물 출력 보고 요청하는 미세조정(이번 세션에 출력1 폭·패킹·폰트 여러 번 조정함).
- **등록(생산앱 푸시) 미세 항목**: 동결건조/동결텐더 → 생산앱 category 정확 enum 미확정(현재 둘 다 freezeDry 매핑). 생산앱 enum 확인되면 분리.

→ **다음 세션 시작 시 "단가는 협의됐나? 아니면 드래그 정렬/마무리?" 물어보고 진행.**

## 4. 주의 (이번 세션에서 확인된 것)
- **규칙 배포 = 재고관리 한 곳만(DL-040).** recipesss `firestore.rules`는 **참고용 사본**(배너 명시). `firebase.json`에 firestore 타깃 **없음**(이미 제거됨, `a0b21a3`). 새 컬렉션 권한 필요 시 → 이 파일에 반영 후 **재고관리에 전달**. 라이브 진실 = Firebase 콘솔 규칙 탭. **Claude는 규칙 배포 직접 시도 금지**(분류기도 차단함).
- **"됐다 안됐다"의 정체 = 배포 누락**(보안 구멍 아님). git push ≠ hosting 배포 ≠ 규칙 배포. 새 컬렉션 추가하면 규칙 배포(재고관리 경유)를 잊지 말 것.
- **firebase CLI 전역 미설치** → `npx firebase-tools ...` 써야 함(`firebase` 명령은 없음).
- **mojibake 주의**: 한글 문자열 커밋 전 grep(`[\x{FFFD}]|濡|쒓`).
- **DOM 직접조작 1건뿐, `allow if true` 0건** — 구조 건강함(이번에 감사함).
- **vestigial**: `RecipeDraft.standardId`는 매트릭스 개편(전 표준 평가) 후 미사용. `/recipes/draft/:draftId` 라우트는 placeholder로 남아있음(상세는 `/recipes/:draftId`).

## 5. 소통 (CLAUDE.md)
한국어·직설·간결. 질문 시 추천+근거. 블로커 vs 개선 구분. 큰 변경(SPEC 결정 뒤집기)은 surface 후 진행. 추측보다 **실데이터/실코드로 검증**(이번에 USDA 비타민D, 프리셋 0값 백업 대조 등으로 오판 막음).

---

*세션 끝. origin/main = `a0b21a3`. 블로커 = recipesssOrders 규칙(재고관리 배포). 다음 = 호두가 단가협의/드래그정렬/마무리 중 선택.*

---

# 부록 A. 다음 세션 상세 인계 (호두 요청 — 두껍게)

> 이 부록은 "호두를 처음부터 아는 것처럼" 시작하기 위한 것. 위 §0~5가 사실,
> 이 부록은 맥락·의도·취향·작업법이다. 다 읽고 시작해라.

## A-1. 호두(jieun@fantapet.com)가 누구이고 뭘 원하는가

- **코딩을 1도 모르는 일반인**이다. 바이브코딩으로 이 앱을 만든다. 코드를 직접
  읽지/쓰지 못한다. 대신 **"어떻게 굴러가야 하는지"는 정확히 안다** — 시스템·데이터·
  업무 흐름을 이해하고 AI를 통제한다. (실력 상위권. `if true` 같은 위험 0건, React+TS
  strict, 테스트 173개, DOM 직접조작 1건 — 구조가 건강하다.)
- **펫푸드(생식·동결건조) 제조사 대표/실무자**다. 이 앱(recipesss = 레시피 계산기)은
  **고양이·강아지 사료 레시피의 영양을 계산하고, 발주서를 뽑고, 생산앱으로 레시피를
  넘기는** 실제 사업 도구다. 장난감이 아니라 **매주 실무에 쓴다.**
- **궁극 목표**: 기존 v2(Vanilla JS 구버전)를 이 React 앱으로 **완전히 대체**하는 것.
  그래서 출력물(PDF 발주서)이 v2 실물과 똑같아야 하고, 영양 계산이 정확해야 한다.

## A-2. 3~5앱 생태계 (이걸 모르면 사고 친다)

- 같은 Firebase 프로젝트 `fant-e5ae5`를 **재고관리·생산관리·운영관리·레시피계산기
  (이 앱)·재무관리**가 공유한다. Firestore 규칙은 **프로젝트당 1개**.
- **규칙 정본·배포 = 재고관리(fantapet-inventory) repo 한 곳만**(DL-040, 공식 공지).
  recipesss는 절대 규칙 배포 안 한다. 이게 그동안 "갑자기 접근 거부" 반복의 원인이었고
  이제 해결됨. **너(Claude)도 규칙 배포 직접 시도 금지**(분류기가 막는다).
- recipesss는 `recipes` 컬렉션에 **등록(푸시)만** 한다(생산앱이 읽음). 생산앱 실제
  스키마와 맞춰야 함(§4.2/§6.6, DL-037). 영양제 제외·active:false로 부분 생성.

## A-3. 호두의 작업 방식 / 취향 (이번 세션에서 파악)

- **"다음 가자" / "OK 다음" 식으로 빠르게 진행**한다. 한 기능 끝나면 바로 다음.
  매번 게이트 통과시키고 커밋·push하고 "배포해서 눈으로 확인"하는 리듬.
- **시각 검증을 한다.** 스크린샷 찍어서 "이거 이상해", "너무 넓어", "깨졌어" 식으로
  피드백. → UI는 **컴팩트·정보밀도 높게**를 선호. 이번에 매트릭스·목록·확정값·PDF를
  여러 번 "더 좁게, 한눈에" 요청했다. 새 UI 만들 땐 처음부터 촘촘하게.
- **정확성에 민감.** "돌아가는 것과 맞는 건 다르다"를 안다. 프리셋 값이 이상하면
  "시드가 잘못된 거 아니냐"고 묻는다 → **추측하지 말고 실데이터(backups/*.json)·
  실API로 대조**해라. 이번에 USDA 비타민D, 프리셋 0값을 그렇게 잡았다.
- **"왜 됐다 안됐다 그래?"** 류의 질문엔 → 보통 **배포 누락**(git push ≠ hosting ≠
  규칙배포)이 원인. 명확히 갈라서 설명해라. 호두는 배포 개념을 헷갈려한다.
- **한국어, 직설, 간결.** 칭찬·사과 군더더기 싫어함. 블로커 vs 개선 구분해서.
  질문할 땐 항상 **추천+근거**. 큰 변경은 surface 후.
- **firebase CLI 전역 미설치** → 항상 `npx firebase-tools`. `firebase` 명령 안 됨.

## A-4. 지금 앱이 할 수 있는 것 (풀사이클 — 다 작동, 발주저장만 막힘)

1. **원료 마스터**(`/ingredients`): USDA에서 영양값 가져오기(검색→더보기→적용/신규),
   수동 입력, 병합, 가나다 정렬, "이 원료 쓰는 레시피" 표시.
2. **레시피**(`/recipes` → 클릭 → `/recipes/:draftId`): 신규 생성, 이름·종·카테고리
   (생식/동결건조/동결텐더)·상태 편집, 구성 원료 편집(추가/삭제/중량/순서/생산단위),
   **영양 매트릭스**(종별 전 표준 동시 평가, 부족/초과 색), **확정값(보장성분)** 입력,
   생산앱 **등록**.
3. **발주**(`/orders`): 프리셋 체크 → 발주요약(sticky) → **프리셋 정리(크기순·투입량
   백필)** 버튼 → **출력 미리보기** / **오늘 날짜로 발주 저장**(← 규칙 배포 대기).
4. **PDF 출력**(`/print`): 출력1(제품별 난각분, 좌우 패킹), 출력2(난각분+치환명),
   저장된 발주 드롭다운(최신순) 재출력, 파일명 = `날짜 영양제-대표용/직원전달용.pdf`.

## A-5. 🔴 제일 먼저 할 일 (순서대로)

1. **`pwd` 확인** (`C:\dev\recipesss`, OneDrive면 멈춤) → **`git pull`**.
2. **블로커 해결 확인**: 호두에게 묻기 —
   "재고관리(fantapet-inventory) 정본에 `recipesssOrders` 규칙 추가하고 배포하셨어요?"
   - 했다 → 발주 저장 작동하는지 호두가 테스트. PrintPage 빨간 에러 사라졌는지.
   - 안 했다 → §2의 블록을 다시 안내(재고관리에 전달). recipesss에선 배포 ❌.
3. **다음 작업 정하기** — 호두에게 추천+근거로 물어라. 후보(§3):
   - 단가관리(`/prices`) = 생산앱 협의 필요(DL-024) → **"단가 인터페이스 생산앱이랑
     협의됐어요?"** 먼저 확인. 안 됐으면 못 함.
   - 드래그 정렬(↑↓ → @dnd-kit), 백업·복원 다듬기, PDF/발주 미세조정.
   - **추천**: 단가가 협의 안 됐으면 → 드래그 정렬이나 호두가 실물 PDF 보고 주는
     미세조정부터. 큰 기능은 거의 다 끝났으니 polish 단계.
4. 그 외엔 **호두가 스크린샷+피드백 주는 대로** 작은 수정 반복(이번 세션 패턴).
   매번 게이트→mojibake grep→커밋→push.

## A-6. 절대 하지 마라 / 꼭 해라
- ❌ firestore 규칙 배포(너도, recipesss에서도). ❌ `firebase` 명령(npx 써라).
- ❌ 추측으로 "데이터 이상" 단정 → 실파일/실API 대조. ❌ 큰 UI를 널널하게 — 촘촘히.
- ✅ 세션 시작 git pull. ✅ 커밋 전 mojibake grep. ✅ 게이트 4종 통과 후 커밋.
- ✅ 질문은 추천+근거. ✅ 배포 안 한 걸 "안 된다"로 오인하지 말고 갈라 설명.

*— 작성: 이전 세션 Claude. origin/main=`a0b21a3`+이 문서. 블로커=recipesssOrders 규칙.*
